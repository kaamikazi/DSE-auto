# Final Expanded-Batch Human Approval Evidence

This is a review pack, not an approval. Qualification remains **0/60**. Dataset,
rule, strategy, campaign, session, proposal, order, transaction, and fill activation
are outside this work. `ma_crossover` was not executed or promoted.

## Scope and disposition

The exact batch is `GP`, `ACI`, `BRACBANK`, `IDLC`, `LANKABAFIN`, `BATBC`,
`SQURPHARMA`, `RENATA`, `POWERGRID`, `SUMITPOWER`, `BERGERPBL`, and `OLYMPIC`.
Every symbol is `conflict_review_required`; every inclusion decision defaults to
`REJECTED / NOT GRANTED`. The 13 previously secondary symbols remain outside the
batch with no permission granted. DSEX remains a separate rejected benchmark input.

The pack contains 96 blank human decision fields (eight per symbol), detailed
symbol profiles, provisional source roles, lifecycle gaps, inactive candidate tiers,
portfolio-diversity diagnostics, all 87,191 unresolved cross-source comparisons,
and all 4,204 corporate-action candidates. It preserves 225 invalid rows as rejected,
128,140 rows as held for review, 273 tier-1 candidates, and 6,871 tier-2 candidates.
No candidate tier is active.

## Conservative source hierarchy

Primary roles require at least 252 observations for the symbol. Eligible sources are
then ranked by registered quality, usable coverage, conflict burden, and invalid-row
burden. This prevents one-day AmarStock files from being treated as primary solely
because their registered score is marginally higher. The provisional adjusted
primary for all 12 symbols is the long-coverage Mendeley end-of-day dataset. That
100% source concentration is itself a review risk, not an endorsement. All source
roles require human approval and retain third-party provenance, licensing, conflict,
and lifecycle caveats.

Common observed coverage is 2009-01-12 through 2026-01-22; observation bounds are
not verified listing bounds. Calendar gaps are not asserted to be missing exchange
sessions or suspensions. Official listing, delisting, suspension, and corporate-action
evidence remains pending. Survivorship risk is therefore high. The largest equal-
weight sector exposure is 25%; minimum liquidity-field availability is 0.814467.
No performance data was used.

## Evidence and rendering

The authoritative local pack is
`reports/final_batch_approval/review_1a1d23469e1c22d1c24cf5a6/`. Its canonical
manifest hash is
`593d1c7766abef6e6df4995be5f05ab2e66fe254774e9d5193433772bde649bb`.
JSON, CSV, and Markdown outputs were generated. A single bounded portable-HTML
builder attempt stopped fail-closed because its static-chart reader remained in
fallback state; no HTML approval pack is claimed.

Protected database counts and strategy-governance state were identical before and
after generation. The canonical audit event hashes the pre-audit evidence files;
`audit_record.json` and `manifest.json` are deliberately post-audit, non-circular
records. Earlier draft-generation events remain immutable and are superseded by the
final schema-v2 event.
