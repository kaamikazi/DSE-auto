# DSE Execution Model

The paper broker supports configurable ticks/bands, minimum liquidity, queue penalties, volume participation, partial fills, settled-share checks, suspension handling and limit-band blocking. It never shorts or leverages. Pessimistic is default; balanced and optimistic are research alternatives. Configuration values are simulation assumptions, not asserted exchange rules.
