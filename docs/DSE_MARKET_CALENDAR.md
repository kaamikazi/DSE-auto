# DSE Market Calendar

`config/dse_market_calendar.yaml` uses `Asia/Dhaka`. Friday/Saturday are configured weekends. Periods are operator assumptions—not authoritative hours—and must be verified before operation. Import holidays from the example CSV. Emergency closure overrides all periods. Decisions are auditable; only continuous trading permits order conversion.
