# Provider Recovery

`bdshare 1.2.1` is installed. TLS verification remains enabled. On 2026-07-13, `dsebd.org` failed certificate-chain verification and `dsebd.com.bd` failed DNS resolution. No `verify=False` path exists. HTTPS endpoint overrides and an optional custom CA bundle are supported through configuration.

`pip install bdfinance` returned “No matching distribution found.” The repository’s local module is marked as a typing facade and is explicitly rejected at runtime, preventing false provider-health claims.

Until public providers recover, operator-attested CSV is the approved validation source. Generic CSV remains research-only.
