# DSEX data

DSEX uses the same governed registration, hashing, normalization, timestamp provenance, duplicate detection, missing-day analysis, and cross-source comparison as equity data. The normalized symbol is `DSEX`.

A stock basket is never silently substituted for DSEX. Any proxy must be explicitly named and excluded from claims about official index performance.
