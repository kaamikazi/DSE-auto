# Portfolio Import Guide

Preview holdings or transactions before commit. Validation errors and hashes identify malformed and duplicate imports. Source files are never changed. A committed batch can be reversed with compensating append-only entries. Unmatched symbols, cash and corporate actions require operator review.
