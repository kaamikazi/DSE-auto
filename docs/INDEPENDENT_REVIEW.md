# Independent Review

Reviewer invitations grant read-only evidence access plus review decisions; they grant no configuration writes. Assignments bind a subject to an evidence hash, expire, record comments/decisions, and require re-review if evidence changes.

Conflict declarations and independence are visible. Operator reviewer remains proposed reviewer but is `non_independent` whenever also operator. Tests use synthetic identities and do not imply an external person participated.
